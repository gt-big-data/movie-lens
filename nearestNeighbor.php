<?php
	function calcSimilarity(aRatings, bRatings) {

	}

	$k = $_GET["k"];
	$idU = mr($_COOKIE['dbUser']); 
	$user = sqlLookupUser(id)
	$best = phparray of k elements
	for($i = 0; $i < results.length; $i++) {
		similarity = calcSimilarity()
	}

?>